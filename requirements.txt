# Core app
streamlit>=1.36,<2
pyyaml>=6.0
requests>=2.31

# Geo stack
numpy>=1.26,<2.0
pandas>=2.2
xarray>=2024.1.0
rasterio>=1.3
rioxarray>=0.15
shapely>=2.0,<3
pyproj>=3.6,<3.7
geopandas>=0.14,<1.0
pyogrio>=0.7         # faster, avoids Fiona/GDAL build pain
Pillow>=10.0
matplotlib>=3.8

# Map rendering
folium>=0.16
streamlit-folium>=0.20
leafmap>=0.38

# Downloads / helpers
gdown>=5.1.0

# Optional: fast tiled raster rendering (enable if you want tile-based map layers)
# localtileserver needs the large-image stack; only enable if the host supports it.
localtileserver>=0.7,<0.9
large-image>=1.21,<2
large-image-source-gdal>=1.21,<2

# Optional: PDF export (requires system libs: cairo, pango, gdk-pixbuf)
# weasyprint>=62.0
